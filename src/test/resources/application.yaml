server:
  port: 8001

spring:
  data:
    mongodb:
      authentication-database: admin
      username: testuser
      password: testpass
      database: userdb
      port: 27001
      host: localhost

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${CLIENT_ID}
            client-secret: ${CLIENT_SECRET}

jwt:
  secret: secret
  expiration: 6200000000
